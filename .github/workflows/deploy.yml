name: Deploy web build to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install love.js
        run: npm install -g love.js

      - name: Build .love archive
        run: |
          zip -r farmbots.love . \
            -x ".git/*" \
            -x ".github/*" \
            -x ".claude/*" \
            -x "libs/love_mcp.lua" \
            -x "libs/love_mcp/*" \
            -x "*.md" \
            -x ".gitignore" \
            -x "web_build/*" \
            -x "dist/*"

      - name: Generate web build
        run: npx love.js -c -m 134217728 -t "FarmBots" farmbots.love web_build

      - name: Fit canvas to viewport
        run: |
          cat >> web_build/theme/love.css <<'EOF'

          /* Scale the game canvas down to fit any viewport while preserving 16:9 aspect. */
          html, body { margin: 0; padding: 0; background: #111; overflow: hidden; width: 100%; height: 100%; }
          h1, footer { display: none !important; }
          #canvas {
            position: fixed !important;
            top: 0; left: 0; right: 0; bottom: 0;
            margin: auto !important;
            width: min(100vw, calc(100vh * 16 / 9)) !important;
            height: min(100vh, calc(100vw * 9 / 16)) !important;
            display: block !important;
          }
          EOF

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./web_build

      - name: Deploy to Pages
        id: deployment
        uses: actions/deploy-pages@v4
